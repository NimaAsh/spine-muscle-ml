# Dataset Metadata

This directory intentionally contains only lightweight derived metadata:

- `stats.json`: normalization statistics and build flags used by training/inference scripts
- `generic_muscle_properties.json`: cached muscle lengths and fiber properties from the generic templates

Raw subject-level dataset files are not redistributed in this public repo snapshot.
